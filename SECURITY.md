# Security Policy

## 报告安全问题

如果你发现安全问题，请不要在公开 Issue 中披露可复现利用细节。请通过项目维护者指定的私有渠道报告，并包含：

- 受影响版本或提交。
- 复现步骤。
- 影响范围。
- 建议修复方向（如有）。

## 运行安全建议

- 生产环境必须覆盖默认管理员密码和所有真实凭据。
- 不要把 `.env`、SQLite 数据库、日志或真实卡密提交到仓库。
- 内部任务接口若需要跨主机调用，请设置强随机 `MERCHANT_INTERNAL_WORKER_TOKEN`。
- 请将服务部署在可信反向代理之后，并启用 HTTPS。
